import 'package:flutter/material.dart';

class TodolistComponent extends StatelessWidget {
  const TodolistComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}