import 'package:flutter/material.dart';

class FrameComponent extends StatelessWidget {
  const FrameComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}